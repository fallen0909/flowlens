# 瀑光 FlowLens

![瀑光 FlowLens 项目封面](docs/assets/flowlens-hero.png)

> 把网页里的图片和视频，一键整理成沉浸式全屏瀑布流。

瀑光 FlowLens 是一个把网页看图、看视频体验做顺手的浏览器小工具。它会把网页里零散、分页、尺寸不一的图片和视频收拢起来，整理成全屏瀑布流，让你像刷本地相册一样连续浏览、放大查看、切换上一张下一张，必要时还可以打包下载。

很多多图网页的问题都很具体：广告和正文混在一起，缩略图太小，下一页要反复点，点开大图会丢失浏览位置，视频和图片混在一起不方便筛选。瀑光做的就是把这些干扰拿掉，把注意力还给图片本身。

![FlowLens 功能图标](docs/assets/flowlens-feature-strip.svg)

## ✨ 正式安装入口

当前统一版本：`v1.4.21`。

普通使用只需要认准两个入口：

- 💻 电脑端：`flowlens-desktop.user.js`
- 📱 手机端：`flowlens-mobile-all.user.js`

安装页：`https://fallen0909.github.io/flowlens/`

电脑端和手机端共用 `src/core`、`src/patches` 下的同一批核心功能；手机端只额外加载 `src/mobile/mobile-center.js` 做布局和触控适配。

## 🖼️ 实际效果

![瀑光 FlowLens 图片流实际效果](docs/assets/flowlens-gallery-preview.png)

打开网页后，点击右下角“瀑光”入口，页面会切换成一个全屏图片流：

- 📊 顶部显示已收集数量、图片/视频数量、下载入口和设置入口。
- 🌊 图片按瀑布流密集排布，一屏能看到更多内容。
- 🎬 视频卡片有播放标识，可与图片分开筛选。
- 🔍 点击图片或视频进入大图模式。
- ▶️ 大图模式支持幻灯片自动切换。
- 🧪 设置面板支持复制分析日志，便于排查图片加载失败、筛选异常和大图切换问题。

## 🚀 功能亮点

![瀑光 FlowLens 图标](docs/assets/flowlens-icon-source.png)

- 🧲 一键进入全屏图片流，不用在原网页里和广告、分页、布局较劲。
- 🎞️ 支持图片和类 GIF 视频：`jpg`、`png`、`webp`、`avif`、`gif`、`mp4`、`webm`、`mov`、`m4v`。
- 🔀 顶部按钮可在“全部 / 图片 / 视频”之间快速切换。
- ⌨️ 大图模式支持滚轮、方向键、侧边箭头切换。
- ▶️ 大图模式新增幻灯片按钮，可按设置速度自动切换下一张大图。
- 🔎 大图可在“适应屏幕”和“1:1 原尺寸”之间切换。
- 🖐️ 原尺寸模式下可拖动查看细节。
- 📦 支持打包下载和链接导出。
- 🧪 支持一键复制分析日志，便于后续定位兼容问题。
- 📱 手机端支持触控浏览、窄屏和折叠屏布局优化。

## 🧪 分析日志

如果遇到这些问题：

- 某些网站图片加载不出来；
- 某些图片被误判为广告或被屏蔽；
- 图/视频筛选不符合预期；
- 大图幻灯片不能正常切换；
- 手机端或电脑端性能异常；

可以打开设置面板，点击“复制日志”，把日志内容发出来。日志会包含当前页面地址、浏览器信息、FlowLens 状态、图片/视频卡片数量、失败媒体记录和运行错误信息。

## 🧭 使用方式

### 💻 电脑端

1. 安装 Tampermonkey。
2. 打开 `flowlens-desktop.user.js`。
3. Tampermonkey 弹出安装页后点击安装或更新。
4. 打开普通网页，点击右下角“瀑光”入口。

### 📱 手机端

1. 使用 Android Edge、Kiwi、Firefox 等支持脚本管理器的浏览器。
2. 安装 Tampermonkey。
3. 打开 `flowlens-mobile-all.user.js`。
4. 安装后打开网页，点击“瀑光”入口。

## 📁 目录说明

```text
flowlens-desktop.user.js      电脑端正式油猴入口
flowlens-mobile-all.user.js   手机端正式油猴入口
src/                          电脑和手机共用源码
  core/                       核心逻辑
  patches/                    共用功能补丁
  mobile/                     手机端专属适配
flowlens-extension/           Chrome / Edge 扩展版备用源码
docs/                         说明图片和展示资源
scripts/                      本地开发辅助脚本
```

## 🧩 版本策略

- 电脑端和手机端版本号保持一致。
- 两端共用功能放到 `src/core` 或 `src/patches`。
- 手机端专属交互放到 `src/mobile`。
- 正式入口只保留 `flowlens-desktop.user.js` 和 `flowlens-mobile-all.user.js`。
- 不再新增 `flowlens-mobile-v*`、`stable`、`latest` 之类的临时安装入口，避免误装旧版本。

## 📝 近期更新

### v1.4.21

- 大图切换速度改为跨网页继承，默认 1.2 秒，并把几个速度挡位整体调快。
- 1:1 原尺寸只在原图确实大于当前适配尺寸时放大，避免小图和高 DPR 手机屏幕闪一下。
- 手机端横向退出改为先退大图、再从边缘内滑退出图片流，减少和 Android 原生返回手势冲突。
- 手机端设置面板改为右上小弹层并限制高度，右上幻灯片按钮与爱心按钮对齐。
- 深色模式顶部补黑底，减少顶部安全区白线。
- 修复 x.810114 / Twitter 视频缩略图失败后 blob 兜底的截帧状态复位。
- 过滤 zttaotu 第 2 页后服务端返回的敏感占位图，避免二维码打码图混入图片流。

### v1.4.19

- 修复 155zy / 155picpic 这类文章页图片采集为空的问题。
- 图片加载失败时增加油猴 blob 兜底，减少跨域或防盗链导致的空图。
- 爱心保存只下载当前最佳图片地址，避免一次点击触发多个候选文件下载。

### v1.4.18

- 修复幻灯片按钮在图片切换时闪烁的问题。
- 1:1 原尺寸查看时立即居中，并暂停自动切换。
- 视频大图会等待当前视频播放完毕后再自动切换。
- 修复 x.810114 / Twitter 视频缩略图加载失败时只显示播放占位的问题。

### v1.4.17

- 修复顶部图/视频切换按钮与设置筛选状态不同步的问题。
- 视频大图模式保留红心按钮，并支持保存视频地址。
- 大图幻灯片改为调用核心切换接口，避免自动切换失效。

### v1.4.15

- 新增设置面板“分析日志”功能，可一键复制问题排查信息。
- 日志包含页面、浏览器、图片/视频卡片、加载失败和运行错误信息。
- 电脑端和手机端同步更新。

### v1.4.14

- 修复大图幻灯片按钮点击后不自动切换的问题。
- 幻灯片切换改为触发下一张卡片的原生打开逻辑，不再依赖右箭头或模拟键盘。
- 恢复完整 README 项目介绍。

### v1.4.13

- 优化性能，取消常驻扫描。
- 设置面板做手机端紧凑化。
- 图/视频切换改回调用插件原生筛选逻辑。
